class Account {
  var username;
  var password;
  var role;

  void Username(username) {
    this.username = username;
  }

  void Password(password) {
    this.password = password;
  }

  void Role(role) {
    this.role = role;
  }
}
